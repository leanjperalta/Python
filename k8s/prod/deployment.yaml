apiVersion: apps/v1
kind: Deployment
metadata:
  name: create-user-api
  namespace: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: create-user-api
  template:
    metadata:
      labels:
        app: create-user-api
    spec:
      nodeSelector:
        kubernetes.io/hostname: knode1.garrahan.gov.ar
      containers:
        - name: api
          image: ghcr.io/leanjperalta/python-create-user-api:fbdd736b531b5d30bc9831d8f5321ffa6f23e1b7
          ports:
            - containerPort: 8000
          env:
            - name: LP_PASS
              valueFrom:
                secretKeyRef:
                  name: api-secrets
                  key: lp_pass
            - name: DDS_PASS
              valueFrom:
                secretKeyRef:
                  name: api-secrets
                  key: dds_pass
